export interface User {
    id: string
    userName: string
    firstName: string
    lastName: string
    email: string
    password: string
    confirmPassword: string
    isActive: boolean
    emailConfirmed: boolean
    phoneNumber: string
    profilePictureUrl: string
}
